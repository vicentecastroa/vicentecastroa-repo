

**TAREA 02**

**clases**

Lista:

Nodo:

Puerto:

Conexion:


**metodos**

MAIN
01. saber_cuando_parar(lista): cada 5000 iteraciones revisa si se han pasado por todas las
conexiones posibles un mínimo de 10 veces.

02. imprimir_red(lista): retorna el archivo red.txt con la lista de todos los puertos y conexiones existentes

03. ruta_a_bummer(lista): retorna un archivo con las conexiones que se deben realizar para llegar lo antes posible a bummer



DATOS

04. add_nodo(nodo): agrega un nodo al último nodo creado.

05.